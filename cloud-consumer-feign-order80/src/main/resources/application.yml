server:
  port: 80
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka, http://eureka7002.com:7002/eureka

ribbon: #超时设置
  ReadTimeout:  5000
  ConnectTimeout: 5000

logging:
  level:
    com.st.springcloud.service.PaymentFeignService: debug